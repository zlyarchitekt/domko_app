fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.6.0
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
alembic==1.14.0
httpx==0.27.2
shapely==2.0.6
geojson==3.1.0
ezdxf==1.4.4
pytest==8.3.3
pytest-asyncio==0.24.0

# Multipart file uploads (POST /api/v1/footprint/import-dxf) — F1-01
python-multipart==0.0.12

# Solar analysis (services/solar_analysis.py) — F4-09
pvlib==0.11.1
pandas==2.2.3
numpy==2.1.2

# WT communication validation, corridor-distance Dijkstra — F3-01 (Faza 2)
networkx==3.4.2

# Optimizer GA branch, services/optimizer.py — F5-07
# NOTE: scipy.optimize.milp was imported but never called (dead import, removed
# in F5-07) — the "LP" branch is actually a heuristic brute-force search, not a
# real linear program. Re-add scipy here if F5-01 is reworked into a real MILP.
pymoo==0.6.1.3

# PDF export — F6-05 (developer decision: reportlab over weasyprint, no system
# GTK/Cairo dependency needed on Windows dev machines)
reportlab==4.2.5
